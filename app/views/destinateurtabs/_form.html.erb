<%= form_with(model: destinateurtab) do |form| %>
  <% if destinateurtab.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(destinateurtab.errors.count, "error") %> prohibited this destinateurtab from being saved:</h2>

      <ul>
        <% destinateurtab.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :descriptionch, style: "display: block" %>
    <%= form.text_field :descriptionch %>
  </div>

  <div>
    <%= form.label :servicetab_id, style: "display: block" %>
    <%= form.text_field :servicetab_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
